5. Multiple Exceptions Handling

You are given two integers and an array.

Perform division and access an array element.

Handle both:

ArithmeticException
ArrayIndexOutOfBoundsException

Example:

Input
a = 10, b = 0, index = 5

Output:
Cannot divide by zero

6. Custom Exception (Age Validation)

Create a custom exception InvalidAgeException.

If age < 18, throw exception.

Example:

Input:
age = 16

Output:
InvalidAgeException: Age must be 18 or above

7. Finally Block Execution

Write a program to show that finally block always executes.

Example Output:

Try block executed
Finally block executed
8. Throw Keyword Usage

You are given a number n.

If n is negative, throw an exception.

Example:

Input:
n = -5

Output:
Exception: Number cannot be negative